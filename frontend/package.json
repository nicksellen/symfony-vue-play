{
  "private": true,
  "name": "frontend",
  "description": "My mind-blowing Vue app",
  "version": "0.0.0",
  "repository": {},
  "scripts": {
    "test": "echo lol",
    "test:unit": "vbuild -c vbuild.unit.js",
    "lint": "vbuild --eslint",
    "dev": "vbuild -d",
    "build": "vbuild"
  },
  "devDependencies": {
    "offline-plugin": "^4.6.1",
    "vbuild": "^6.0.0",
    "vbuild-karma": "^1.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.15.3"
  }
}
